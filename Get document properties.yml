info:
  name: Get document properties
  type: http
  seq: 3

http:
  method: GET
  url: https://api.protectedpdf.com/api/2.0/Version/GetByExternalKey/test123
  headers:
    - name: X-VITR-SESSION-TOKEN
      value: "{{SessionToken}}"
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-VITR-ACCOUNT-TOKEN
      value: "{{VITR_ACCOUNT_TOKEN}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
